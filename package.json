{
  "type": "module",
  "scripts": {
    "prebuild": "esbuild src/browser.js --bundle --minify --outfile=dist/files/libs.js",
    "build": "node build.js",
    "publish": "npm run build && neocities push public"
  },
  "devDependencies": {
    "basename": "^0.1.2",
    "esbuild": "^0.13.3",
    "esm": "^3.2.25",
    "glob": "^7.1.6",
    "markdown-it": "^11.0.0",
    "markdown-it-front-matter": "^0.2.1",
    "shelljs": "^0.8.4"
  },
  "license": "WTFPL"
}
